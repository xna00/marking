{
  "name": "marking",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "rm -r dist/doc; tsc && npm run cp && zip -r dist/doc/extension.zip dist/extension && npm run add-hash",
    "watch": "npm-watch",
    "cp": "cp doc/index.html dist/doc/; cp doc/test/index.html dist/doc/test/; cp -r doc/test/images dist/doc/test/; cp doc/*.png dist/doc/; cp -r extension/images dist/extension; cp extension/popup.html dist/extension/popup.html",
    "add-hash": "node add-hash.ts",
    "rename-to-mjs": "node rename-js-to-mjs.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "watch": {
    "build": {
      "patterns": ["extension", "doc"],
      "extensions": "js,ts,html,png,json",
      "quiet": false
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "@types/wicg-file-system-access": "^2023.10.7",
    "npm-watch": "^0.13.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/uzip": "^0.20201231.2"
  }
}
